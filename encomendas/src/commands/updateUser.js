// eslint-disable-next-line no-unused-vars
const { Client, Message } = require('discord.js')

module.exports = {
  names: ['updateUser', 'uu', 'updateU'],
  /**
   *
   * @param {Client} client
   * @param {string[]} args
   * @param {Message} message
   */
  exe: function (client, args, message) {

  }
}
